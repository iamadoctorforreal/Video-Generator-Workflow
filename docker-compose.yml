version: '3.8'

services:
  video-generator:
    build: .
    container_name: kokoro-video-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
